name: Automation Recon (24/7)

on:
  schedule:
    # Menggunakan menit ganjil untuk menghindari jam sibuk antrean GitHub
    - cron: '13,33,53 * * * *' 
  workflow_dispatch:

jobs:
  recon-job:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    strategy:
      fail-fast: false
      matrix:
        # Menjalankan 3 sniper sekaligus secara paralel (Aman & Kencang)
        sniper_id: [1, 2, 3, 4, 5]
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Cache Seen URLs
        uses: actions/cache@v3
        with:
          path: .seen_urls
          key: seen-urls-cache-${{ github.run_id }}
          restore-keys: |
            seen-urls-cache-

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '>=1.20'

      - name: Install Tools
        run: |
          sudo apt-get update
          # Pasang Google Chrome agar Katana bisa langsung pakai (System Chrome)
          sudo apt-get install -y google-chrome-stable dnsutils
          go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
          go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
          go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest
          go install -v github.com/projectdiscovery/katana/cmd/katana@latest
          go install -v github.com/tomnomnom/assetfinder@latest
          go install -v github.com/tomnomnom/waybackurls@latest
          go install -v github.com/lc/gau/v2/cmd/gau@latest
          go install -v github.com/tomnomnom/anew@latest
          echo "$HOME/go/bin" >> $GITHUB_PATH
          nuclei -ut -silent

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Python Dependencies
        run: pip install requests

      - name: Run Predator Recon
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          H1_USERNAME: ${{ secrets.H1_USERNAME }}
          H1_API_KEY: ${{ secrets.H1_API_KEY }}
          TG_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CRIT: ${{ secrets.TELEGRAM_CRITICAL_ID }}
          TG_GEN: ${{ secrets.TELEGRAM_GENERAL_ID }}
          TG_DATA: ${{ secrets.TELEGRAM_DATA_ID }}
        run: |
          # 1. SETUP NOTIF CONFIG (Silent)
          echo "telegram:" > notify-config.yaml
          echo "  - id: \"p1_p2_alerts\"" >> notify-config.yaml
          echo "    telegram_apikey: \"$TG_TOKEN\"" >> notify-config.yaml
          echo "    telegram_chatid: \"$TG_CRIT\"" >> notify-config.yaml
          echo "    format: \"{{data}}\"" >> notify-config.yaml

          # 2. ANTI-COLLISION DELAY
          SLEEP_TIME=$(( (${{ matrix.sniper_id }} - 1) * 40 ))
          echo "⏳ Sniper ${{ matrix.sniper_id }} preparing deep-scan tools..."
          sleep $SLEEP_TIME

          # 3. TARGET SELECTION (SEQUENTIAL NO-OVERLAP)
          ls targets/*.txt | sort > targets_sorted.tmp
          TOTAL_TARGETS=$(wc -l < targets_sorted.tmp)
          RUN_NUM=${{ github.run_number }}
          SNIPER_ID=${{ matrix.sniper_id }}
          SNIPER_COUNT=5 
          
          INDEX=$(( ((RUN_NUM - 1) * SNIPER_COUNT + (SNIPER_ID - 1)) % TOTAL_TARGETS + 1 ))
          target_file=$(sed -n "${INDEX}p" targets_sorted.tmp)
          program_name=$(basename "$target_file" .txt)
          
          echo "🎯 Sniper $SNIPER_ID Engaging Mission #$RUN_NUM: $program_name"
          mkdir -p "data/$program_name"
          touch .seen_urls

          # --- [STEP 1: SUBDOMAIN DISCOVERY] ---
          echo "[+] Step 1: Gathering Subdomains..."
          subfinder -dL "$target_file" -all -silent -o "data/$program_name/s1.txt" > /dev/null 2>&1 || true
          while read -r domain; do
            echo "$domain" | assetfinder --subs-only >> "data/$program_name/s1.txt" 2>/dev/null || true
          done < "$target_file"
          cat "data/$program_name/s1.txt" | sed 's/*.//g' | tr -d ' ' | sort -u > "data/$program_name/subdomains.txt"

          if [ -s "data/$program_name/subdomains.txt" ]; then
            # --- [STEP 2: LIVE CHECK] ---
            echo "[+] Step 2: Filtering Live Hosts..."
            timeout 20m httpx -l "data/$program_name/subdomains.txt" -silent -ip -td -mc 200,301,302,401,403,405 -H "User-Agent: NovaRecon/2026" -o "data/$program_name/active_hosts.txt" > /dev/null 2>&1 || true
            
            if [ -s "data/$program_name/active_hosts.txt" ]; then
                # --- [STEP 3: DEEP SIGHT (Katana + Wayback) - ULTRA FIX] ---
                echo "[+] Step 3: Mining All Hidden Endpoints..."
                touch data/$program_name/urls.txt
                touch data/$program_name/katana_urls.txt
                
                # 1. Ambil sejarah URL
                cat data/$program_name/active_hosts.txt | waybackurls > data/$program_name/urls.txt 2>/dev/null || true
                cat data/$program_name/active_hosts.txt | gau --subs >> data/$program_name/urls.txt 2>/dev/null || true
                
                # 2. Crawler Aktif (Katana) - PAKSA PAKE GOOGLE CHROME BAWAAN GITHUB
                echo "    (Crawling with Katana Headless...)"
                # Kita arahkan path-nya ke Chrome bawaan GitHub Actions
                export PATH=$PATH:/usr/bin/google-chrome
                timeout 25m katana -list data/$program_name/active_hosts.txt \
                  -hl -system-chrome -no-sandbox \
                  -jc -kf all -d 3 -silent -o data/$program_name/katana_urls.txt || true
                
                # 3. Gabungkan
                cat data/$program_name/urls.txt data/$program_name/katana_urls.txt | sort -u > data/$program_name/all_endpoints.txt
                
                # Fallback sakti
                if [ ! -s "data/$program_name/all_endpoints.txt" ]; then
                  cp data/$program_name/active_hosts.txt data/$program_name/all_endpoints.txt
                fi
                
                echo "    ✅ Done. Total unique endpoints found: $(wc -l < data/$program_name/all_endpoints.txt)"

                # --- [STEP 4: NUCLEI 2.0 (THE KILLSHOT)] ---
                echo "[+] Phase 1: Deep Scanning ALL Endpoints..."
                # Kita tembak SEMUA endpoint yang ditemukan, bukan cuma pintu depan!
                timeout 45m nuclei -l "data/$program_name/all_endpoints.txt" \
                  -severity critical,high,medium \
                  -et dns,ssl,whois -es info \
                  -rl 150 -c 70 -mhe 100 -no-color -silent \
                  -H "User-Agent: NovaRecon/2026" \
                  -jsonl -o "data/$program_name/nuclei_results.json" > /dev/null 2>&1 || true

                # --- [STEP 5: SECRET LEAKS SCAN] ---
                echo "[+] Phase 2: Scanning for Sensitive Tokens..."
                timeout 15m nuclei -l "data/$program_name/all_endpoints.txt" \
                  -t http/exposed-tokens/ \
                  -H "User-Agent: NovaRecon/2026" \
                  -rl 80 -no-color -mhe 100 -silent -jsonl -o "data/$program_name/token_results.json" > /dev/null 2>&1 || true
                [ -f "data/$program_name/token_results.json" ] && cat "data/$program_name/token_results.json" >> "data/$program_name/nuclei_results.json"

                # --- [STEP 6: AI ANALYSIS] ---
                echo "[+] Step 4: AI Analysis & H1 Drafting..."
                PROGRAM_NAME=$program_name python scripts/validate.py > /dev/null 2>&1 || true

                # --- [STEP 7: TELEGRAM & BACKUP] ---
                if [ -d "data/$program_name/alerts/high" ]; then
                  for f in data/$program_name/alerts/high/*.md; do
                    [ -e "$f" ] || continue
                    MSG_HIGH=$(cat "$f")
                    curl -s -X POST "https://api.telegram.org/bot$TG_TOKEN/sendMessage" -d "chat_id=$TG_CRIT" -d "text=$MSG_HIGH" > /dev/null 2>&1
                  done
                fi

                zip -qj "${program_name}_FULL_DATA.zip" data/$program_name/*.txt data/$program_name/*.json .seen_urls || true
                curl -s -F chat_id="$TG_DATA" -F document=@"${program_name}_FULL_DATA.zip" -F caption="📦 DATABASE: $program_name" "https://api.telegram.org/bot$TG_TOKEN/sendDocument" > /dev/null 2>&1
            fi
          fi

      - name: Sync Database to Repo
        run: |
          git config --local user.email "bot@sniper-recon.ai"
          git config --local user.name "Recon-Sniper-Bot"
          git add .seen_urls
          git commit -m "database: update seen_urls [skip ci]" || exit 0
          git push
